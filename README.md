# cleanux
Bash script to clean up temporary files (#, ~) on linux system

# how to use it ?
As root (or with 'sudo'), simply type the command 'cleanux' to clean every files from the current directory. 
You can also specify the path of the folder to clean files from like : 'cleanux /etc/'.
